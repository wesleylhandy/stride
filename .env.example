# Stride Environment Variables
# 
# Copy this file to .env and configure your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# The .env file is automatically loaded by Docker Compose.

# =============================================================================
# Database Configuration
# =============================================================================

# Database password (used by PostgreSQL container and connection strings)
# REQUIRED for production - use a strong password
DB_PASSWORD=stride_dev_password

# Database connection URL
# For Docker Compose, use the service name 'stride-postgres' as host
# For local development, use 'localhost:5433'
DATABASE_URL=postgresql://stride:${DB_PASSWORD}@stride-postgres:5432/stride

# =============================================================================
# Application Configuration
# =============================================================================

# Environment mode: development | production
NODE_ENV=development

# Public URL where the application is accessible
# For production, use your actual domain with HTTPS
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Application version (optional, used for error tracking)
APP_VERSION=

# =============================================================================
# Authentication (REQUIRED)
# =============================================================================

# JWT secret for token signing (min 32 characters)
# Generate with: openssl rand -hex 32
# REQUIRED - must be set in production
JWT_SECRET=your-secret-key-change-in-production

# Session secret for encryption (min 32 characters)
# Generate with: openssl rand -hex 32
# REQUIRED - must be set in production
SESSION_SECRET=your-session-secret-change-in-production

# =============================================================================
# AI Gateway Configuration (Optional)
# =============================================================================

# AI Gateway service URL (used by web application)
AI_GATEWAY_URL=http://ai-gateway:3001

# Local LLM endpoint (for self-hosted LLMs like Ollama)
LLM_ENDPOINT=http://localhost:11434

# Commercial LLM API Keys (use one or the other, not both)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# =============================================================================
# Email Configuration (Optional)
# =============================================================================

# SMTP server for sending emails (email verification, notifications, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-password
SMTP_FROM=noreply@your-domain.com

# =============================================================================
# OAuth Configuration (Optional)
# Required for repository integration with GitHub/GitLab
# =============================================================================

# GitHub OAuth (for GitHub repository integration)
# Get credentials from: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# GitLab OAuth (for GitLab repository integration)
# Get credentials from: https://gitlab.com/-/profile/applications
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=

# =============================================================================
# Rate Limiting (Optional)
# =============================================================================

# Enable rate limiting (true/false)
RATE_LIMIT_ENABLED=true

# Maximum requests per window
RATE_LIMIT_MAX_REQUESTS=100

# Time window in milliseconds
RATE_LIMIT_WINDOW_MS=60000

# =============================================================================
# Logging Configuration (Optional)
# =============================================================================

# Log level: debug | info | warn | error
LOG_LEVEL=info

# Log format: json | pretty
LOG_FORMAT=json

# =============================================================================
# Error Tracking (Optional)
# =============================================================================

# Sentry DSN for error tracking
# Get from: https://sentry.io/
SENTRY_DSN=

# Enable error tracking (true/false)
ERROR_TRACKING_ENABLED=false
