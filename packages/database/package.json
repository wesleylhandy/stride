{
  "name": "@stride/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "bash scripts/migrate.sh",
    "db:studio": "prisma studio",
    "db:deploy": "bash scripts/migrate.sh deploy",
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^7.2.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "prisma": "^7.2.0",
    "typescript": "^5",
    "@types/pg": "^8.10.0",
    "dotenv": "^16.4.5"
  }
}

