# API Contracts

This feature is entirely client-side and does not require API contracts.

All keyboard shortcuts interact with existing API endpoints through existing component handlers.

No new API endpoints are needed.
